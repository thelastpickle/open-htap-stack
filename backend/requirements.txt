# HTAP Mission Control backend
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.4
pydantic-settings==2.15.0

# Cassandra — the Apache DataStax Python driver; 3.29 is the first release with
# CQL vector support, which the AI search needs.
cassandra-driver==3.30.1

# PrestoDB DBAPI client
presto-python-client==0.8.4

# Spark Thrift Server (HiveServer2); auth=NONE, so no SASL wrapper is needed
pyhive==0.7.0
thrift==0.24.0

# Outbound HTTP for the optional embedding and NL-to-SQL endpoints
httpx==0.28.1

# The SQL engine for the cqlite path.  This pin and the datafusion and
# datafusion-ffi pins in cqlite-datafusion/Cargo.toml are one pin, not three:
# the reader hands this library a capsule holding a repr(C) struct, and the two
# sides must agree on its layout.  The cqlite_datafusion wheel beside it is built
# from cqlite-datafusion/ by the first stage of backend/Dockerfile.
#
# This line is the binding side of the three.  The Rust crates are published at
# 55.0.0, which carries arrow 59; this wheel is published only to 54.0.0.  So 54 is
# the highest the Rust side may take, and DataFusion 55 waits on PyPI rather than on
# anything in this repository.  Raise all three together or none of them.
datafusion==54.0.0
